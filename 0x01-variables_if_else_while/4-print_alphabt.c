#include <stdio.h>
/**
  * main- prints the alphabet in lowercase
  * Return: Always 0(Success)
  */
int main(void)
{
	;

	int c = 'a';

	while
	(c <= 'Z')
	{


			if
				((c != 'q' && c != 'e') && c <= 'Z');

			putchar(c);
			c++;
	}
	putchar('\n');
	return (0);
}
