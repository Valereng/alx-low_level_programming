#include <stdio.h>
/**
  *main- prints the alphabet in lowercase 
  *Return: Always 0(Success)
  */
int main(void)
{
	char C;

	c = 'a';
	while
	(c <= 'Z') {
		if ((c != 'q' && c != 'e') && c <= 'Z')
			 
			puchar(c);
	              c++;
	}
      puchar('\'n')
      return (0);
}      
